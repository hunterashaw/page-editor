{
    "devDependencies": {
        "browser-sync": "^2.27.10",
        "esbuild": "^0.15.10",
        "npm-run-all": "^4.1.5",
        "preact": "^10.11.1",
        "sirv-cli": "^2.0.2",
        "tailwindcss": "^3.1.8"
    },
    "dependencies": {
        "preact-render-to-string": "^5.2.5"
    },
    "scripts": {
        "start": "run-p watch:app watch:style serve",
        "serve": "npx browser-sync start --server 'dist' --files 'dist' --no-open --ws",
        "watch:app": "npx esbuild src/index.jsx --bundle --sourcemap=inline --format=iife --outfile=dist/builder.js --jsx-import-source=preact --jsx=automatic --watch",
        "watch:style": "npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch"
    }
}
